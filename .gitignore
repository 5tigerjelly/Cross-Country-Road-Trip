# Photo/video ORIGINALS — 2.4GB of HEIC/MOV, some over GitHub's 100MB limit,
# and they carry exact GPS (incl. home) in EXIF. The web-converted, GPS-stripped
# copies live in site/media/ and are committed instead.
photos/

# Hotel booking vouchers (name, address, confirmation numbers)
hotels/

# Cached exiftool dump — regenerated by the pipeline
build/

# macOS cruft
.DS_Store
**/.DS_Store

# Local Claude Code state
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Python venv (pipeline deps installed locally per PEP-668)
venv/
.venv/
